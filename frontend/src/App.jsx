import React,{useState} from 'react';\nimport ChatRoom from './components/ChatRoom';\nexport default function App(){\n  const [room,setRoom]=useState(''); const [joined,setJoined]=useState(false);\n  const [anonId] = useState(()=>"anon_"+Date.now().toString(36));\n  return joined? <ChatRoom room={room} anonId={anonId}/> : (\n    <div style={{padding:20}}>\n      <h3>Join a chatroom</h3>\n      <input value={room} onChange={e=>setRoom(e.target.value)} placeholder="room name"/>\n      <button onClick={()=>room&&setJoined(true)}>Join</button>\n    </div>\n  );\n}